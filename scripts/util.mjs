// Re-export from canonical source to avoid duplication
export {
  getConfig,
  assertNodeVersion,
  joinUrl,
  httpJson,
  readStdinJson,
  ok,
  fail,
  nowIso,
  newSessionId,
} from '../src/lib/common.mjs';
